import type {Moeda} from '../types/api'; export const formatCurrency=(v:number|string|undefined,c:Moeda|string='BRL')=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:c}).format(Number(v||0)); export const formatOptionalCurrency=(v:number|string|null|undefined,c:Moeda|string='BRL')=>v==null?'—':formatCurrency(v,c); export const formatNumber=(v:number|string|undefined)=>new Intl.NumberFormat('pt-BR',{maximumFractionDigits:8}).format(Number(v||0)); export const formatPercent=(v:number|string|null|undefined)=>v==null?'—':`${new Intl.NumberFormat('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2}).format(Number(v))}%`; export const formatDate=(v:string|undefined)=>v?new Intl.DateTimeFormat('pt-BR',{dateStyle:'short',timeStyle:'short'}).format(new Date(v)):'—';
